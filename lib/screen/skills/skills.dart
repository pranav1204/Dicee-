import 'package:flutter/material.dart';
import 'package:mi_profile/screen/skills/body.dart';

class Skills extends StatelessWidget {
  const Skills({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Body();
  }
}
